# TaskMaster Dashboard - Environment Configuration

# ====================
# Required Variables
# ====================

# Path to the project containing .taskmaster/ directory
# This is where your TaskMaster tasks.json and issues are located
PROJECT_ROOT=/path/to/your/project

# ====================
# Optional Variables
# ====================

# Server Configuration
PORT=5000
NODE_ENV=development

# TaskMaster Paths (relative to PROJECT_ROOT)
TASKMASTER_PATH=.taskmaster
ISSUES_PATH=.taskmaster/issues

# Feature Flags (Phase 1: all false)
ENABLE_EDITING=false              # Enable file editing (Phase 3+)
ENABLE_GIT_ACTIONS=false          # Enable git push/pull (Phase 3+)
ENABLE_MCP_MANAGEMENT=false       # Enable MCP server start/stop (Phase 3+)

# WebSocket Configuration (Phase 2+)
WS_HEARTBEAT_INTERVAL=30000       # 30 seconds
WS_TIMEOUT=60000                  # 60 seconds

# Terminal Configuration (Phase 2+)
TERMINAL_ROWS=30
TERMINAL_COLS=120
TERMINAL_SCROLLBACK=10000

# Git Configuration (Phase 3+)
GIT_COMMIT_LIMIT=10               # Number of commits to show

# Logging
LOG_LEVEL=info                    # error | warn | info | debug
